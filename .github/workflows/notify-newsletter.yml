name: Notify newsletter on new content

on:
  push:
    branches: [main]
    paths:
      - 'src/content/blog/**'
      - 'src/content/academy/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (fetch-depth 2 to compare HEAD~1)
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Run notify script
        env:
          # SITE_URL come repo variable (Settings → Variables → Actions).
          # Fallback su ricetteperpippe.com se la variable non è impostata.
          SITE_URL: ${{ vars.SITE_URL || 'https://ricetteperpippe.com' }}
          # SEND_RECIPE_TOKEN come repo secret (Settings → Secrets → Actions).
          # Stesso valore esatto della env var Netlify SEND_RECIPE_TOKEN.
          SEND_RECIPE_TOKEN: ${{ secrets.SEND_RECIPE_TOKEN }}
        run: node scripts/notify-newsletter.mjs
