name: Deploy to Netlify via push

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Netlify handles deployment
        run: echo "Netlify auto-deploys on push to main"
